interface NodeProps {
  key: number;
  col: number;
  row: number;
  isBegining: boolean;
  isEnd: boolean;
  isWall: boolean;
};

const Node = ({ key, col, row, isBegining, isEnd, isWall }:NodeProps) => {
  return(
    <>
    </>
  )
}
export default Node;